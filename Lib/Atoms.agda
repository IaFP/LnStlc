module Atoms where

-- TODO!
-- postulate
--   Atom      : Set
--   =dec      : ∀ (x y : Atom) → Dec (x ≡ y)
--   atomFresh : ∀ (xs : List Atom) → ∃[ a ] ( a ∉ xs )
--   fresh     : List Atom → Atom
--   fresh∉    : ∀ (xs : List Atom) → (fresh xs) ∉ xs
--   toNat     : Atom → ℕ

-- Atoms : Set
-- Atoms = List Atom

